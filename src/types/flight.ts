export type Flight = {
  id: string;
  airline: string;
  origin: string;
  destination: string;
  departureTime: string;
  arrivalTime: string;
  stops: number;
  price: number;
  currency: string;
  duration: string;
};